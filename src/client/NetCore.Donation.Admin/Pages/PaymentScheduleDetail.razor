@page "/payment-schedules/{Id:guid}"
@inject DonationApiClient Api
@inject NavigationManager Nav

<PageTitle>Payment schedule — Donation admin</PageTitle>

<div class="d-flex align-items-center gap-3 mb-3">
    <h1 class="mb-0">Payment schedule</h1>
    <RadzenButton Text="Back to list" ButtonStyle="ButtonStyle.Light" Size="ButtonSize.Small" Click="@(() => Nav.NavigateTo("payment-schedules"))" />
</div>

@if (!string.IsNullOrWhiteSpace(error))
{
    <div class="alert alert-danger">@error</div>
}
else if (loading)
{
    <p>Loading…</p>
}
else if (item is null)
{
    <p class="text-muted">Payment schedule not found.</p>
}
else
{
    <div class="detail-card">
        <DetailGrid>
            <DetailField Label="Identifier">@RadzenODataQuery.IdentifierOrShort(item.Identifier, item.Id)</DetailField>
            <DetailField Label="Amount">@item.Amount.ToString("C")</DetailField>
            <DetailField Label="Payment type">@item.PaymentType</DetailField>
            <DetailField Label="Book date">@item.BookDate</DetailField>
            <DetailField Label="Interval">@item.RecurringInterval</DetailField>
            <DetailField Label="Recurring">@item.IsRecurring</DetailField>
            <DetailField Label="Contact">
                <EntityLink Path="contacts" Id="item.ContactId" Text="@item.ContactFullName" />
            </DetailField>
            <DetailField Label="Payment method">
                <EntityLink Path="payment-methods" Id="item.PaymentMethodId" Text="@item.PaymentMethodDisplayName" />
            </DetailField>
        </DetailGrid>
    </div>
}

@code {
    [Parameter] public Guid Id { get; set; }

    private bool loading = true;
    private string? error;
    private PaymentScheduleDto? item;

    protected override async Task OnParametersSetAsync()
    {
        loading = true;
        try
        {
            item = await Api.GetPaymentScheduleAsync(Id);
            error = null;
        }
        catch (Exception exception)
        {
            error = exception.Message;
            item = null;
        }
        finally
        {
            loading = false;
        }
    }
}
